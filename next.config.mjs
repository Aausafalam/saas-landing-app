/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "templates.envytheme.com","demos.pixinvent.com"], // Add the allowed hostname here
    },
};

export default nextConfig;
